import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["plus.unsplash.com","images.unsplash.com"], // add all external image hosts you use
    
  },
};

export default nextConfig;
